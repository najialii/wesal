import { render } from '@testing-library/react';
import { 
  Table, 
  TableBody, 
  TableCaption, 
  TableCell, 
  TableFooter, 
  TableHead, 
  TableHeader, 
  TableRow 
} from '../table';

describe('Table Components', () => {
  it('should render all table components without errors', () => {